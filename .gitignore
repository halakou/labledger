# Build state — regenerated on every run and restored from the Actions cache.
# Never commit these: they hold Telegram message ids and fetched assets.
.desk-posted.json
.desk-archive.json
.desk-open.json
.desk-assets/
dist-site/
node_modules/

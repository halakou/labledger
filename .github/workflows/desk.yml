name: desk

# Live desk is pages.yml (RSS → Cloudflare Pages → Telegram).
# This leftover ingest ping has no DESK_URL and must not run on the hour.
on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Skip leftover ingest
        run: echo "Live automation is pages.yml. This workflow is retired."

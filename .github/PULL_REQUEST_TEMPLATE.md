## What this changes

A one-sentence summary of the change.

## Why

The reason it should be filed. If it corrects a brief, link the brief and the
primary source that shows the correct fact.

## Verification

- [ ] `node --test test/` passes
- [ ] `node scripts/check-repo.mjs` passes
- [ ] If markup changed, the built output in `dist-site/` was checked

## The rules

- [ ] Nothing is invented — this repeats only what an official source said
- [ ] No new dependency, no paid tier, no new top-level directory
      (or the allow-list in `scripts/check-repo.mjs` was updated and why is below)
